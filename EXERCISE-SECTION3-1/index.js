const detachedNum = prompt("Masukan Angka")

if (detachedNum % 2 == 0) {
    alert(`Angka ini ${detachedNum} adalah bilangan genap`)
} else {
    alert(`Angka ini ${detachedNum} adalah bilangan ganjil`)
}